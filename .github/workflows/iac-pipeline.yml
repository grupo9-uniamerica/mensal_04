name: IaC Pipeline

on:
  push:
    branches:
      - master

jobs:
  infra:
    name: Provision Infra
    runs-on: ubuntu-latest
    outputs:
      vm_ip: ${{ steps.ip.outputs.vm_ip }}

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Criar chave.json
        run: |
          mkdir -p terraform
          echo "${{ secrets.GCP_CREDENTIALS }}" | base64 --decode > terraform/chave.json

      - name: Debug chave.json (remova depois de testar)
        run: |
          head terraform/chave.json

      - name: Criar SSH public key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PUBLIC_KEY }}" > ~/.ssh/id_rsa.pub

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: '1.6.0'

      - name: Set GOOGLE_APPLICATION_CREDENTIALS
        run: echo "GOOGLE_APPLICATION_CREDENTIALS=$GITHUB_WORKSPACE/terraform/chave.json" >> $GITHUB_ENV

      - name: Init Terraform
        run: cd terraform && terraform init

      - name: Plan Terraform
        run: cd terraform && terraform plan

      - name: Apply Terraform
        run: cd terraform && terraform apply -auto-approve

      - name: Get VM IP
        id: ip
        run: |
          echo "🚀 Iniciando captura do IP da VM..."
          cd terraform
          echo "📂 Diretório atual: $(pwd)"
          
          echo "🔍 Executando terraform output..."
          terraform output -raw public_ip > ip_raw.txt
          echo "📝 Conteúdo do ip_raw.txt:"
          cat ip_raw.txt
          
          echo "🧹 Limpando o output..."
          IP=$(cat ip_raw.txt | tr -d '\n' | tr -d '\r')
          echo "📝 IP após limpeza: '$IP'"
          
          if [ -z "$IP" ]; then
            echo "❌ Erro: IP está vazio"
            exit 1
          fi
          
          echo "💾 Salvando IP em arquivo..."
          echo "$IP" > $GITHUB_WORKSPACE/vm_ip.txt
          echo "📝 Conteúdo do vm_ip.txt:"
          cat $GITHUB_WORKSPACE/vm_ip.txt
          
          echo "📤 Configurando output do job..."
          echo "vm_ip=$IP" >> $GITHUB_OUTPUT
          echo "✅ IP capturado e configurado com sucesso: $IP"

      - name: Apagar chave.json
        run: |
          echo "🗑️ Removendo chave.json..."
          rm terraform/chave.json
          echo "✅ chave.json removida"

  deploy:
    name: Configure VM
    needs: infra
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Setup Ansible
        run: |
          echo "🚀 Configurando Ansible..."
          sudo apt update
          sudo apt install -y ansible sshpass
          echo "✅ Ansible instalado"
          ansible --version

      - name: Setup SSH key
        run: |
          echo "🔑 Configurando chave SSH..."
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          echo "✅ Chave SSH configurada"
          ls -la ~/.ssh/

      - name: Debug VM IP
        run: |
          echo "🔍 Verificando IP da VM..."
          echo "IP do output do job anterior: '${{ needs.infra.outputs.vm_ip }}'"
          echo "📝 Conteúdo do vm_ip.txt:"
          cat $GITHUB_WORKSPACE/vm_ip.txt

      - name: Configurar Inventory
        run: |
          echo "📋 Configurando inventory do Ansible..."
          
          # Lê o IP do arquivo
          VM_IP=$(cat $GITHUB_WORKSPACE/vm_ip.txt)
          echo "📝 IP lido do arquivo: '$VM_IP'"
          
          # Verifica se o IP é válido
          echo "🔍 Validando formato do IP..."
          if ! echo "$VM_IP" | grep -qE '^[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+$'; then
            echo "❌ Erro: IP inválido recebido: '$VM_IP'"
            exit 1
          fi
          echo "✅ IP válido"
          
          # Cria o inventory
          echo "📝 Criando arquivo inventory.ini..."
          cat > ansible/inventory.ini << EOF
          [webserver]
          $VM_IP ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa
          EOF
          echo "✅ Inventory criado"

      - name: Debug Inventory
        run: |
          echo "🔍 Verificando inventory..."
          echo "📝 Conteúdo do inventory.ini:"
          cat ansible/inventory.ini
          echo "📝 Permissões do inventory.ini:"
          ls -la ansible/inventory.ini

      - name: Testar conexão SSH
        run: |
          echo "🔌 Testando conexão SSH..."
          VM_IP=$(cat $GITHUB_WORKSPACE/vm_ip.txt)
          echo "📝 IP para conexão: '$VM_IP'"
          
          echo "🔑 Verificando chave SSH..."
          ls -la ~/.ssh/id_rsa
          
          echo "🔄 Tentando conexão SSH..."
          ssh -v -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null ubuntu@$VM_IP 'echo "Conexão SSH bem sucedida!"'
          if [ $? -eq 0 ]; then
            echo "✅ Conexão SSH estabelecida com sucesso!"
          else
            echo "❌ Falha na conexão SSH"
            echo "🔍 Últimas linhas do log SSH:"
            tail -n 20 ~/.ssh/ssh.log 2>/dev/null || echo "Log SSH não disponível"
            exit 1
          fi

      - name: Run Ansible Playbook
        run: |
          echo "🚀 Executando playbook Ansible..."
          echo "📝 Conteúdo do playbook:"
          cat ansible/playbook.yml
          
          echo "🔄 Iniciando execução..."
          ansible-playbook -i ansible/inventory.ini ansible/playbook.yml -vvv
          if [ $? -eq 0 ]; then
            echo "✅ Playbook executado com sucesso!"
          else
            echo "❌ Falha na execução do playbook"
            exit 1
          fi

